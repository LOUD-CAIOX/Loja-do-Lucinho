-- create a table
CREATE TABLE students (
  id INTEGER PRIMARY KEY,
  name TEXT NOT NULL,
  gender TEXT NOT NULL
);
-- insert some values
INSERT INTO students VALUES (1, 'CPF', 'M');
INSERT INTO students VALUES (2, 'RG', 'F');
-- fetch some values
SELECT * FROM students WHERE gender = 'F';
